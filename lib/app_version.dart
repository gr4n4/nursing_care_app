/// 이 앱의 배포 버전.
///
/// 병동에서 "지금 쓰시는 게 몇 버전이에요?"를 물어볼 수 있어야 해서 화면에
/// 띄운다. 오류 제보를 받았을 때 어느 시점의 코드인지 알아야 원인을 좁힐 수
/// 있고, 간호사가 옛 버전을 캐시로 붙들고 있는 것인지도 이 값으로 가른다.
///
/// **pubspec.yaml 의 version 과 같은 값을 유지할 것.**
/// 배포할 때는 세 가지를 함께 올린다 — 여기, pubspec.yaml, 그리고 git 태그.
/// 자세한 절차는 BUILD.md 의 '배포 순서'를 볼 것.
///
/// 규칙(유의적 버전):
///   2.x.x  앞자리 — 간호과에 넘긴 큰 배포. '2차 버전'이 2.0.0 이다.
///   x.1.x  가운뎃자리 — 기능이 늘었을 때
///   x.x.1  뒷자리 — 버그만 고쳤을 때
const String appVersion = '2.0.2';
